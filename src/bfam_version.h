#ifndef BFAM_VERSION_H
#define BFAM_VERSION_H

#include <bfam_base.h>

/** Basic function that returns the version number.
 *
 * \return A pointer to a string literal containing the version number.
 */
const char * bfam_version_get();

#endif
